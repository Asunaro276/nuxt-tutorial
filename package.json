{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxi build",
    "dev": "nuxi dev",
    "generate": "nuxi generate",
    "preview": "nuxi preview",
    "postinstall": "nuxi prepare",
    "start": "pm2 start .output/server/index.mjs",
    "generate:api": "npx openapi-typescript openapi/api.yml --output ./gen/api.gen.ts"
  },
  "devDependencies": {
    "@types/node": "^18",
    "nuxt": "^3.5.2"
  },
  "dependencies": {
    "openapi-typescript": "^6.2.6",
    "pm2": "^5.3.0"
  }
}
